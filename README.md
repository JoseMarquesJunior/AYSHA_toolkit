# AYSHA_toolkit
# ⚙️ Extrator de Add-Ons L5X (Streamlit)

Aplicativo web simples em **Python + Streamlit** que lê arquivos **.L5X / XML** de CLPs Rockwell e gera um **Excel consolidado por tipo de Add-On**.

---

## 🚀 Como usar localmente

```bash
# 1️⃣ Instale dependências
pip install -r requirements.txt

# 2️⃣ Execute o app
streamlit run app.py
git